#include<stdio.h>
int main()
{
	int no=num,rem,sum=0,rev=1,arm=0,choice;
	switch(choice)
	{
	case EXIT : printf("BYE----\n");
		break;

	case SUM_DIGIT : while(temp !=0)
					{
							rem = temp % 10;
							sum = sum + rem;
							temp = temp/10;

					}
					printf("sum = %d \n",sum);
					break;
	}





return 0;
}
