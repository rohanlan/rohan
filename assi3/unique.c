#include<stdio.h>
int main(void)
{
int i=1,j,fact=1,k;
printf("Enter the value of num \n");
scanf("%d",&num);
while(i<=num)
{
	i++;
	fact=fact*i;
while(j=j/fact)
{
if(fact<=4)
j*i=k;
printf("%d * %d = %d",i,j,k);
}

}

return 0;
}
