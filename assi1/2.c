//get input from user and convert it into octal decimal hexa
#include <stdio.h>
int main()
{
    int a;
    printf("enter any number: ");
    scanf("%d", &a);
    printf("Character value = %c\n decimal value is = %d\n Octal value is = %o\n hexa value is = %x ", a, a, a, a);
    return 0;
}